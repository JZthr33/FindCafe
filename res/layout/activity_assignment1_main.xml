<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".Assignment1MainActivity" >

    <TextView
        android:id="@+id/resulting_places"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentRight="true"
        android:layout_below="@+id/btn_execute"
        android:layout_marginTop="26dp"
        android:text="Resulting Places" />

    <Button
        android:id="@+id/btn_execute"
        style="?android:attr/buttonStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/resulting_places"
        android:layout_below="@+id/inputLocation"
        android:layout_marginTop="16dp"
        android:text="Start" />

    <EditText
        android:id="@+id/inputRadius"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/btn_execute"
        android:layout_alignBottom="@+id/btn_execute"
        android:layout_alignRight="@+id/resulting_places"
        android:ems="10"
        android:text="Radius (i.e. 250)" />

    <EditText
        android:id="@+id/inputLocation"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentRight="true"
        android:layout_below="@+id/appLabel"
        android:layout_marginTop="14dp"
        android:ems="10"
        android:hint="@string/location_lat_lon_"
        android:text="Lat, Lon (i.e. 37.2290, -80.4152)" >

        <requestFocus />
    </EditText>

    <TextView
        android:id="@+id/appLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="26dp"
        android:text="Find a Cafe" />

</RelativeLayout>